# 📡 RED COAST BASE PANEL (BPB-Worker-Concept)

> **⚠ 警报：检测到高频引力波干扰。上行链路丢包率 89.4%。**
> **“不要回答！不要回答！不要回答！”**
> ...除非你是唯一的幸存节点。

![Status](https://img.shields.io/badge/System_Integrity-CRITICAL-red) ![Node_Survival](https://img.shields.io/badge/Active_Nodes-2%2F15-red) ![Clearance](https://img.shields.io/badge/Clearance-Commander-black)

## 🔇 最后的传输 | The Last Transmission

**Red Coast Base Panel** 不是一个普通的 Web 前端。它是为了对抗 **BPB (Block/Purge/Ban)** 协议而构建的最后一道防线。

在这个面板上，你看到的不仅仅是 UI，是生存的倒计时。我们抛弃了 React、Vue 等所有臃肿的现代框架——因为在连接随时被切断的几 KB 贫瘠带宽里，只有**原生 HTML5 + CSS3 + Vanilla JS** 能够穿透智子的封锁。

这里没有 `npm install`，因为每一个多余的依赖包都可能暴露我们的坐标。

## 📉 节点伤亡报告 | Casualty Report

> **实时监控日志 - 2026.01.13 [绝密]**
> *看着这些变灰的节点，就像看着战友一个个倒下。*

* ~~[US-West-01]~~ `104.16.x.x` - **CONFIRMED KILLED** (TCP Reset)
* ~~[SG-Asia-04]~~ `141.101.x.x` - **MIA** (端口 443 无响应)
* ~~[JP-Tokyo-09]~~ `172.67.x.x` - **CORRUPTED** (DNS 污染)
* **[HK-Relay-02]** `104.28.x.x` - **ONLINE (UNSTABLE)** <--- *当前唯一接入点*
* ~~[KR-Seoul-05]~~ - ██████████ (数据已抹除)

**距离当前节点失效预计剩余时间：03:14:05**

## 🛠️ 生存组件 | Survival Kit

为了在严酷的“黑暗森林”打击中存活，本面板集成了以下生存机能：

### 1. 辐射残留级 CRT 视效
这不是简单的复古滤镜。这是由于长期在高频雷达波下工作，视网膜受损产生的视觉残留。
* **特性**：扫描线干扰、RGB 色彩分离、屏幕微光。
* **作用**：模糊现实与虚拟的边界，让你忽略连接不稳定的焦虑。

### 2. 智子盲区发生器 (Sophon Shield)
点击 Dashboard 的 `DEPLOY SHIELD`。
* **原理**：播放一段心跳倒计时动画。
* **真相**：虽然这只是 CSS 动画，但在倒计时的 3 秒钟里，你可以欺骗自己——你是安全的。

### 3. 多语言伪装协议 (i18n)
为了在不同管辖区生存，系统支持一键切换伪装语言：
* **🇬🇧 English (UN)**：伪装成联合国环境署监测终端。
* **🇨🇳 中文 (红岸)**：恢复红岸基地内部代号（极度危险，仅限内部）。
* **🇷🇺 Русский (SU)**：苏维埃特供，用于西伯利亚链路。
* **🇮🇷 فارسی (IR)**：波斯语 RTL 支持，用于中东地下网络。

## 📂 系统架构 | File System

```text
/RED-COAST-BASE
│
├── index.html      # 躯壳：唯一的入口。包含 HTML 结构。
├── style.css       # 幻觉：负责制造 CRT 扫描线和绝望的红色幽灵。
├── script.js       # 神经：处理逻辑、翻译和虚假的 Ping 值。
└── README.md       # 遗书：你现在正在读的这份文档。

```

## 🏴‍☠️ 紧急部署 | Emergency Protocol

**不要使用常规 CI/CD 流水线！** 每一个构建步骤产生的特征指纹都会引来打击。

1. **静默下载**：获取源码包（小于 50KB）。
2. **寻找掩体**：找一个还活着的静态托管（Cloudflare Pages, Vercel... 或者藏在床底下的树莓派）。
3. **直接上传**：无需编译，直接推送。
4. **祈祷**：等待绿灯亮起。

## ⚙️ 绝密档案 | Classified Docs

核心逻辑位于 `script.js`。为了规避审查，我们对端口进行了特殊处理：

```javascript
// 那些曾经开放的端口，现在大多是死路一条
// 请根据当前战况，手动解开注释
const tlsPorts = [
    443,        // 极度危险
    ~~8443~~,   // 已确认被封
    2053,       // 存活
    ~~2083~~,   // 丢包率 100%
    2087, 
    2096
]; 

```

*如果你的 IP 被封，请不要联系开发者。开发者已经去冬眠了，醒来大概是 400 年后。*

---

<div align="center">
<h3>🌍 我们的星星是孤独的。</h3>
<p><i>System uptime: 0 days 0 hours 42 minutes.</i></p>
<p>Connection Quality: <span style="color:red">POOR</span> | Morale: <span style="color:red">LOW</span></p>
<p>Made with 🩸 and 💻 by the <b>Red Coast Engineering Dept</b>.</p>
</div>

```

```
